{{-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profil Siswa</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
</head>

<body>
    <div class="dashboard">
        <h1>Profil Siswa</h1>
        <div class="profile">
            <img src="{{ asset('storage/' . $student->photo) }}" alt="Foto Siswa" id="student-photo">
            <div class="info">
                <p>Nama: <span id="siswa-nama">{{ $siswa->nama }}</span></p>
                <p>Kelas: <span id="siswa-kelas">{{ $siswa->kelas }}</span></p>
                <p>Jenis Kelamin: <span id="siswa-jenis_kelamin">{{ $siswa->jenis_kelamin }}</span></p>
                <p>Phone: <span id="siswa-phone">{{ $siswa->phone }}</span></p>
                <p>Alamat: <span id="siswa-alamat">{{ $siswa->alamat }}</span></p>
            </div>
            <a href="{{ route('siswa.edit', $siswa->id) }}" id="update-button">Update</a>
        </div>
    </div>
</body>

</html> --}}
